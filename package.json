{
  "name": "gpt-5.6-router-cli",
  "version": "0.2.0",
  "private": true,
  "description": "A local Codex CLI router for GPT-5.6 Luna, Terra, and Sol.",
  "type": "module",
  "scripts": {
    "setup": "./install.sh",
    "start": "node server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
